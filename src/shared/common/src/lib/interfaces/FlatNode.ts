export interface IFlatNode {
    // expandable: boolean;
    // name: string;
    // key: string;
    // level: number;

    expandable: boolean;
    id: string;
    label: string;
    level: number;
    loading?: boolean;
  }
  