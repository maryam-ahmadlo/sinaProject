export interface IFlatNode {
    // expandable: boolean;
    // name: string;
    // key: string;

    expandable: boolean;
    id: number;
    label: string;
    level: number;
    loading?: boolean;
  }
  